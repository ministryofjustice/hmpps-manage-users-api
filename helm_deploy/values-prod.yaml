---
# Per environment values which override defaults in hmpps-manage-users-api/values.yaml

generic-service:
  replicaCount: 4

  ingress:
    host: manage-users-api.hmpps.service.justice.gov.uk

  env:
    HMPPS_AUTH_ENDPOINT_URL: https://sign-in.hmpps.service.justice.gov.uk/auth
    NOMIS_ENDPOINT_URL: https://nomis-user.aks-live-1.studio-hosting.service.justice.gov.uk
    EXTERNAL_USERS_ENDPOINT_URL: https://external-users-api.hmpps.service.justice.gov.uk
    DELIUS_ENDPOINT_URL: https://community-api-secure.probation.service.justice.gov.uk
    BANNER_ROLES:
    BANNER_EMPTY:
    BANNER_DPSMENU: The original DPS user search has now been removed, the new search provides improved layout and features such as search by email and multi-role filtering.

# CloudPlatform AlertManager receiver to route promethues alerts to slack
generic-prometheus-alerts:
  alertSeverity: hmpps_tech
